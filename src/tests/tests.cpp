// Test neighbor search
/* for (auto& p : m_particles)
{
    // get list of pointers to neighbors of particle
    list<ParticleObject *> lst =
        m_grid.neighbors(p);
    cout << "Position of current particle" << endl;
    cout << p.getPosition().x() << ": "
         << p.getPosition().y() << ": "
         << p.getPosition().z() << endl;
    // iterate over neighbors
    cout << "Positions of neighbors" << endl;
    for (auto const &point : lst)
    {
        cout << point->getPosition().x() << ", "
             << point->getPosition().y() << ", "
             << point->getPosition().z() << endl;
    }
    cout << endl;
}*/
